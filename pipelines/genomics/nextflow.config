// Minimal Nextflow config template for somatic variant calling pipeline
params {
    outdir = "results"
    samplesheet = "samplesheet.csv"
    reference = "/path/to/GRCh38.fasta"
    threads = 8
}

process {
    executor = 'local'
    memory = '8 GB'
}
