# Changelog

## [Unreleased]
- Make `rna_seq/annotate_results.py` configurable and robust to different gene ID column names.
- Add pytest coverage for annotate behavior (index-based, `gene`, and explicit `merge_key`).
- Document `gene_annotation_key` config option for local workflows.
